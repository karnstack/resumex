import type { ResumeTemplateMeta } from '@/lib/template-registry'

export const meta: ResumeTemplateMeta = {
  id: 'executive-graphite',
  name: 'Executive Graphite',
  description:
    'Pure graphite executive. Hierarchy carried by size, weight, and rules only. The quietest of the executive set.',
  tags: ['executive', 'single-column', 'wordmark', 'grayscale', 'minimal'],
  fonts: {
    sans: 'Inter, ui-sans-serif, system-ui, -apple-system, "Segoe UI", Helvetica, Arial, sans-serif',
  },
}
