import type { ResumeTemplateMeta } from '@/lib/template-registry'

export const meta: ResumeTemplateMeta = {
  id: 'executive-monogram',
  name: 'Executive Monogram',
  description:
    'Boardroom executive with a thin-rule monogram crest above the wordmark. Refined initials, restrained dark-gray accent.',
  tags: ['executive', 'single-column', 'monogram', 'wordmark', 'corporate'],
  fonts: {
    sans: 'Inter, ui-sans-serif, system-ui, -apple-system, "Segoe UI", Helvetica, Arial, sans-serif',
  },
}
