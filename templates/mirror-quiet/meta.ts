import type { ResumeTemplateMeta } from '@/lib/template-registry'

export const meta: ResumeTemplateMeta = {
  id: 'mirror-quiet',
  name: 'Mirror Quiet',
  description:
    'The quietest mirror. No vertical rule between columns — just whitespace. Low contrast, light hierarchy.',
  tags: ['two-column', 'sidebar-right', 'sans', 'quiet', 'low-contrast'],
}
