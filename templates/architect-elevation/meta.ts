import type { ResumeTemplateMeta } from '@/lib/template-registry'

export const meta: ResumeTemplateMeta = {
  id: 'architect-elevation',
  name: 'Architect Elevation',
  description:
    'Vertical timeline with year ticks (level markers). Mono level labels. Body to the right of the axis.',
  tags: ['architect', 'technical', 'timeline', 'single-column', 'mono'],
  fonts: {
    sans: 'Helvetica Neue, Helvetica, Arial, ui-sans-serif, system-ui, sans-serif',
    mono: 'JetBrains Mono, ui-monospace, SFMono-Regular, Menlo, monospace',
  },
}
