import type { ResumeTemplateMeta } from '@/lib/template-registry'

export const meta: ResumeTemplateMeta = {
  id: 'indexcard-stamp',
  name: 'Index Card — Stamp',
  description:
    'Library-stamp aesthetic. Rotated date-stamp on the header, return-stamp marks under each entry. Cataloged feel.',
  tags: ['indexcard', 'library', 'stamp', 'single-column'],
  fonts: {
    sans: 'ui-sans-serif, system-ui, -apple-system, "Helvetica Neue", Arial, sans-serif',
    mono: 'ui-monospace, "JetBrains Mono", Menlo, monospace',
  },
}
