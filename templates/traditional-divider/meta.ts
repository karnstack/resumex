import type { ResumeTemplateMeta } from '@/lib/template-registry'

export const meta: ResumeTemplateMeta = {
  id: 'traditional-divider',
  name: 'Traditional · Divider',
  description:
    'Two-column with sidebar on the left and a strong vertical divider rule splitting the columns. The structure does the talking.',
  tags: ['traditional', 'two-column', 'sidebar-left', 'divider'],
  fonts: {
    sans: 'Inter, ui-sans-serif, system-ui, sans-serif',
  },
}
