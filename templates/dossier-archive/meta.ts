import type { ResumeTemplateMeta } from '@/lib/template-registry'

export const meta: ResumeTemplateMeta = {
  id: 'dossier-archive',
  name: 'Dossier — Archive',
  description:
    'Vintage archive-folder dossier. Manila tab marker on the top edge, mono labels, slate-on-cream palette.',
  tags: ['dossier', 'archive', 'single-column', 'vintage', 'manila'],
  fonts: {
    sans: 'Inter, ui-sans-serif, system-ui, sans-serif',
    mono: 'JetBrains Mono, IBM Plex Mono, ui-monospace, SFMono-Regular, Menlo, monospace',
  },
}
