import type { ResumeTemplateMeta } from '@/lib/template-registry'

export const meta: ResumeTemplateMeta = {
  id: 'swiss-mirror',
  name: 'Swiss Mirror',
  description:
    'Swiss / International Style with the eye-flow flipped: section labels right-aligned, accent rail on the right.',
  tags: ['swiss', 'single-column', 'grid', 'mirror', 'color-block'],
  fonts: {
    sans: 'Helvetica Neue, Helvetica, Arial, ui-sans-serif, system-ui, sans-serif',
  },
}
