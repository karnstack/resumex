import type { ResumeTemplateMeta } from '@/lib/template-registry'

export const meta: ResumeTemplateMeta = {
  id: 'display-shout',
  name: 'Display Shout',
  description: 'Full-width edge-to-edge name banner — big contrast moment, quiet body beneath.',
  tags: ['display', 'single-column', 'banner'],
}
