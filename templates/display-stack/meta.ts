import type { ResumeTemplateMeta } from '@/lib/template-registry'

export const meta: ResumeTemplateMeta = {
  id: 'display-stack',
  name: 'Display Stack',
  description: 'Stacked-name treatment — one word per line, each at heavy 900 weight.',
  tags: ['display', 'two-column', 'stacked-name'],
}
