import type { ResumeTemplateMeta } from '@/lib/template-registry'

export const meta: ResumeTemplateMeta = {
  id: 'mirror-block',
  name: 'Mirror Block',
  description:
    'Full-width color block at the top houses the name and contact rail. Below: experience left, sidebar right. No photo, just colorfield identity.',
  tags: ['two-column', 'sidebar-right', 'sans', 'banner'],
}
