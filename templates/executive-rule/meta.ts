import type { ResumeTemplateMeta } from '@/lib/template-registry'

export const meta: ResumeTemplateMeta = {
  id: 'executive-rule',
  name: 'Executive Rule',
  description:
    'Boardroom executive anchored by bold horizontal rules under the wordmark and section heads. Strong structural gravitas.',
  tags: ['executive', 'single-column', 'wordmark', 'rules', 'corporate'],
  fonts: {
    sans: 'Inter, ui-sans-serif, system-ui, -apple-system, "Segoe UI", Helvetica, Arial, sans-serif',
  },
}
