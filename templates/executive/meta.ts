import type { ResumeTemplateMeta } from '@/lib/template-registry'

export const meta: ResumeTemplateMeta = {
  id: 'executive',
  name: 'Executive',
  description:
    'Boardroom executive. Wordmark name in tracked-out caps with a single restrained dark-gray accent. Calm, expensive, plenty of air.',
  tags: ['executive', 'single-column', 'wordmark', 'corporate'],
  fonts: {
    sans: 'Inter, ui-sans-serif, system-ui, -apple-system, "Segoe UI", Helvetica, Arial, sans-serif',
  },
}
