import type { ResumeTemplateMeta } from '@/lib/template-registry'

export const meta: ResumeTemplateMeta = {
  id: 'swiss',
  name: 'Swiss',
  description:
    'Swiss / International Style. Strict baseline grid, all-caps tracked labels, cobalt block accent on a cool gray neutral.',
  tags: ['swiss', 'single-column', 'grid', 'color-block'],
  fonts: {
    sans: 'Helvetica Neue, Helvetica, Arial, ui-sans-serif, system-ui, sans-serif',
  },
}
