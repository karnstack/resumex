import type { ResumeTemplateMeta } from '@/lib/template-registry'

export const meta: ResumeTemplateMeta = {
  id: 'display',
  name: 'Display',
  description: 'Heavy display-sans name in black, thin body in cream — bold/light tension.',
  tags: ['display', 'single-column', 'high-contrast'],
}
