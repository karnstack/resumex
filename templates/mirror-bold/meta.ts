import type { ResumeTemplateMeta } from '@/lib/template-registry'

export const meta: ResumeTemplateMeta = {
  id: 'mirror-bold',
  name: 'Mirror Bold',
  description:
    'Sidebar-right with a strong dark identity block at the top — name reversed on graphite. Main column carries experience.',
  tags: ['two-column', 'sidebar-right', 'sans', 'bold'],
}
