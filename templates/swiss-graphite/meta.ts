import type { ResumeTemplateMeta } from '@/lib/template-registry'

export const meta: ResumeTemplateMeta = {
  id: 'swiss-graphite',
  name: 'Swiss Graphite',
  description:
    'Swiss / International Style in pure grayscale. Hierarchy is built entirely from size, weight, and rule placement.',
  tags: ['swiss', 'single-column', 'grid', 'minimal', 'grayscale'],
  fonts: {
    sans: 'Helvetica Neue, Helvetica, Arial, ui-sans-serif, system-ui, sans-serif',
  },
}
