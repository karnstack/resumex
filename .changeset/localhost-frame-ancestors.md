---
"resumex": patch
---

- Fix: allow `localhost` in `frame-ancestors` so the local `/templates` preview iframe works under the Cloudflare Pages `_headers` policy.
- Docs: README revamp with a fanned paper-stack hero composition, a "build your own" section, and a PR invite for new templates.
